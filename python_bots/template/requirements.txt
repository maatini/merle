# ⚠️ LEGACY — nur noch für manuelle pip-Installationen
#
# Seit Phase 0 wird das Template mit uv verwaltet.
# Bitte verwende:
#
#   uv sync --group dev
#
# Die definitive Abhängigkeitsdefinition steht in pyproject.toml.
# Dieses File wird in zukünftigen Phasen entfernt.

# --- Nur noch als Referenz / Fallback ---
merle-core>=0.1.0
pydantic-settings>=2.0,<3.0
rpaframework>=28.0,<29.0
playwright>=1.40,<2.0
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0
pdfplumber>=0.10,<1.0
loguru>=0.7,<1.0
tenacity>=8.0,<9.0
httpx>=0.25,<1.0
ruff>=0.9,<1.0
mypy>=1.14,<2.0
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-playwright>=0.4,<1.0

